- hosts: all
  become: true
  vars:
    docker_users: [ "vagrant" ]
    docker_install_compose: false
  roles:
    - geerlingguy.docker
  tasks:
    - name: Install docker python
      apt:
        pkg:
        - python3-docker
